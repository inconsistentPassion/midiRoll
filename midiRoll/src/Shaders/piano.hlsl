// piano.hlsl — Piano key rendering
// Placeholder. Currently handled inline by SpriteBatch.
float4 PSMain(float4 pos : SV_POSITION, float2 uv : TEXCOORD0, float4 color : COLOR0) : SV_TARGET {
    return color;
}
