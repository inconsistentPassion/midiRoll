// glow.hlsl — Glow/bloom shader
// Placeholder. Currently handled by Bloom.cpp inline HLSL.
float4 PSMain(float4 pos : SV_POSITION, float2 uv : TEXCOORD0) : SV_TARGET {
    return float4(1, 1, 1, 1);
}
